name: tmp-verify
on: [push]
jobs:
  t:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '24.x'
      - run: npm ci --ignore-scripts
      - run: npx ava test/unittests/engineUtilsTest.js
